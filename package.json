{
  "name": "@opencoredev/cursor-cloud-agent-mcp",
  "version": "1.0.3",
  "description": "Cursor Cloud Agents MCP server — Vercel serverless with streamable HTTP transport. Auth via x-cursor-api-key header.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
